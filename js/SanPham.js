class SanPham {
  constructor(name, price, screen, backCamera, frontCamera, desc, type) {
    this.name = name;
    this.price = price;
    this.screen = screen;
    this.backCamera = backCamera;
    this.frontCamera = frontCamera;
    this.desc = desc;
    this.type = type;
  }
}
export default SanPham;
